# docker-compose.yml
version: '3.8'

services:
  jekyll:
    build: .
    volumes:
      - .:/usr/src/app
    working_dir: /usr/src/app
    ports:
      - "4000:4000"
    command: bundle exec jekyll serve --host 0.0.0.0 --force_polling
